{
  "name": "incident-tracker-monorepo",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "server": "cd server && npm start",
    "docker": "docker compose up --build"
  }
}
